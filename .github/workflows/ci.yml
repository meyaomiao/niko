name: Niko CI

on:
  push:
    branches:
      - main
    paths-ignore:
      - "**/*.md"
      - "brand/**"
      - "website/**"
      - ".github/**"
  pull_request:
    paths-ignore:
      - "**/*.md"
      - "brand/**"
      - "website/**"
      - ".github/**"

concurrency:
  group: niko-ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  rust-check:
    name: Rust cargo check (self-hosted)
    strategy:
      fail-fast: false
      matrix:
        os: [{ name: mac, runs-on: [self-hosted, macOS, niko-macos] }, { name: win, runs-on: [self-hosted, Windows, niko-windows] }]
    runs-on: ${{ matrix.os.runs-on }}
    timeout-minutes: 30
    defaults:
      run:
        working-directory: src-tauri
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri
      - run: cargo check --all-targets

  ts-check:
    name: TypeScript tsc check
    runs-on: [self-hosted, Linux, niko-linux]
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: package-lock.json
      - run: npm ci
      - run: npx tsc --noEmit
